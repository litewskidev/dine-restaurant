import styles from './BookingForm.module.scss';

const BookingForm = () => {
  return(
    <form>
      Name
      Email
      Pick a date
      Pick a time
      <button>Make Reservation</button>
    </form>
  );
};

export default BookingForm;
